<!DOCTYPE html>
<html lang="pt-br" class="h-screen">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Treinamento DTI{% endblock %}</title>
    <link rel="shortcut icon" href="{{ asset('images/coren.png') }}" type="image/x-icon">

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body class="flex flex-col h-screen">

{% block header %}
{% endblock %}

<main class="flex flex-1">

    <!-- Sidebar -->
    <aside class="w-64 bg-primary dark:bg-dark-primary text-white h-full flex flex-col">
        <!-- Logo -->
        <div class="w-full flex items-center justify-center py-4">
            <img src="{{ asset('images/Corensc_branco.png') }}" alt="Logo do Coren-SC" width="120">
        </div>
        <nav class="p-4 flex-1">
            {% block itens_nav %}
            {% endblock %}
        </nav>
        <nav class="p-4 mt-auto pb-6">
{#            {% if app.user %}#}
                <!-- Se o usuário estiver logado -->
{#                <div class="relative">#}
{#                    <button class="px-4 py-2 bg-button-light rounded-md flex items-center justify-around text-primary shadow-md hover:bg-button-hover active:bg-button-hover#}
{#                    transition-all duration-150 ease-in-out transform active:scale-95 active:shadow-lg">#}
{#                        <i class="fa-solid fa-user-circle"></i> Matheus Luz#}
{#                    </button>#}
{#                    <div class="absolute right-0 mt-2 w-48 bg-white text-black rounded-md shadow-lg hidden group-hover:block">#}
{#                        <a href="#" class="block px-4 py-2 text-primary hover:bg-gray-200">Sair</a>#}
{#                    </div>#}
{#                </div>#}
{#            {% else %}#}
                <!-- Se o usuário NÃO estiver logado -->
                <a href="#"
                   class="px-4 py-2 bg-button-light rounded-md flex items-center justify-around text-primary shadow-md hover:bg-button-hover active:bg-button-hover
                    transition-all duration-150 ease-in-out transform active:scale-95 active:shadow-lg">
                    <i class="fa-solid fa-arrow-right-to-bracket"></i> Entrar
                </a>
{#            {% endif %}#}
        </nav>
    </aside>


    <!-- Conteúdo principal -->
    <div class="flex-1 bg-background dark:bg-dark-background flex flex-col text-primary dark:text-dark-text">

        {% block main %}
            <div class="flex-1 p-6">
                Ainda pensando no que colocar aqui!
            </div>
        {% endblock %}

        {% block footer %}
            <!-- Footer -->
            <footer class="text-center py-6">
                <p class="text-sm">© {{ "now"|date('Y') }} Treinamento DTI | Todos os direitos reservados.</p>
            </footer>
        {% endblock %}
    </div>

</main>

</body>
</html>
