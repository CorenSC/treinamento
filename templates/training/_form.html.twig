{{ form_start(form) }}
    <div class="w-full h-screen p-2 flex items-center space-y-12 -mb-24 flex-col">
        <div class="text-primary dark:text-dark-text w-1/2">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
        </div>
        <button class="w-1/2 bg-primary h-12 flex justify-center items-center dark:text-primary text-dark-text dark:bg-button-light dark:hover:bg-button-hover shadow shadow-md rounded-md transition-all duration-150 ease-in-out transform active:scale-95 active:shadow-lg cursor-pointer">Salvar</button>
    </div>


{{ form_end(form) }}
